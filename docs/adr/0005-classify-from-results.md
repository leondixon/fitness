# Classify Sessions from Results, not from the Pick

A completed Session enters Lookback as one Classification taken from Results (or from an inferred run), not from the Quality Check-in asked for. A strength Pick logged as high-rep work counts as hypertrophy. The alternative was tagging by prescription so the Lookback would match the Pick; that was rejected so the record reflects what actually happened.
